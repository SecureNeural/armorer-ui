# Auth
AUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# Database — local SQLite (default) or Turso (production)
DATABASE_URL=file:armorer.db
# DATABASE_AUTH_TOKEN=  # only needed for Turso
